import type { NextConfig } from "next";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
const nextConfig: any = {
  allowedDevOrigins: ["10.10.20.118", "localhost", "127.0.0.1"],
};

export default nextConfig;
